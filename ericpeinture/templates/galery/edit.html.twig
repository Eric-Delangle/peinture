{% extends 'base.html.twig' %}

{% block title %}Edit Galery{% endblock %}

{% block body %}
<div class="container center"> 
    <h1>Edit Galery</h1>

    {{ include('galery/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('picture_new') }}">Ajouter une image</a>
    <a href="{{ path('galery_index') }}">back to list</a>

    {{ include('galery/_delete_form.html.twig') }}
</div>
<div class="container">
        <h3>Vos oeuvres</h3>
        {% for art in galery.picture %}
            <p><img src="{{ asset(['images/products/', art.picture]|join) }}"  style="width: 300px; height: auto;" alt="oeuvre"></p>
            <a class="btn btn-danger placeho" href="{{ path('picture_delete', {'id': picture.id}) }}">Options de cette image</a>
        {% endfor %}
    </div>
{% endblock %}
